[project]
name = "jukebox"
version = "1.1.1"
description = "Shared VPS music library with independent browser playback."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["jukebox"]
